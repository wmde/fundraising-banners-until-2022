language: node_js

node_js:
  - "8"

install: travis_retry npm install

script:
  - npm run lint:js
  - npm run lint:css
  - npm run build

notifications:
  email:
    on_success: change
    on_failure: always

cache:
  directories:
    - "node_modules"
