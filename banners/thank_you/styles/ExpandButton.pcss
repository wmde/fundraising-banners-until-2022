.wmde-banner {
	.expand-button {
		cursor: pointer;
		display: inline-block;
		line-height: 40px;
		padding: 0 20px;
		background: $color-secondary;
		color: $color-white;
		font-weight: bold;
		border-radius: 0 0 6px 6px;
	}
}
